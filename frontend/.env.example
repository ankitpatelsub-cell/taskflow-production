# Set to 'true' if Google OAuth is configured on the backend
VITE_GOOGLE_OAUTH=false

# WebSocket URL (defaults to ws://<host>:3001/ws if not set)
# VITE_WS_URL=ws://localhost:3001/ws

# Sentry frontend DSN
VITE_SENTRY_DSN=
